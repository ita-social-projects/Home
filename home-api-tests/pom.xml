<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <parent>
        <artifactId>home</artifactId>
        <groupId>com.softserveinc.ita.homeproject</groupId>
        <version>${revision}</version>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>
    <artifactId>home-api-tests</artifactId>

    <properties>
        <surefire-version>3.0.0-M5</surefire-version>
        <assertj-version>3.18.1</assertj-version>
        <commons-lang3-version>3.11</commons-lang3-version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj-version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.softserveinc.ita.homeproject</groupId>
            <artifactId>home-java-client</artifactId>
            <version>${revision}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
            <version>${commons-lang3-version}</version>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>api-tests</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <version>${surefire-version}</version>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>


    <properties>
        <fabric8io.docker.maven.plugin.version>0.34.1</fabric8io.docker.maven.plugin.version>
        <docker.jdk.version>openjdk:11-jdk-buster</docker.jdk.version>
        <docker.postgres.version>postgres:latest</docker.postgres.version>
        <postgres.db>postgres</postgres.db>
        <postgres.user>postgres</postgres.user>
        <postgres.password>password</postgres.password>
        <postgres.url>jdbc:postgresql://db-postgres:5432/postgres</postgres.url>
        <home.application.jar.name>home-application</home.application.jar.name>
        <home.application.jar.version>0.0.1-SNAPSHOT</home.application.jar.version>
        <home.data.migration.jar.name>home-data-migration</home.data.migration.jar.name>
        <home.data.migration.jar.version>0.0.1-SNAPSHOT</home.data.migration.jar.version>
        <postgres.port>5433:5432</postgres.port>
        <liquibase.port>8000:8000</liquibase.port>
        <home.application.port>8080:8080</home.application.port>
    </properties>

    <profiles>
        <profile>
            <id>docker</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>io.fabric8</groupId>
                        <artifactId>docker-maven-plugin</artifactId>
                        <version>${fabric8io.docker.maven.plugin.version}</version>
                        <extensions>true</extensions>
                        <configuration>
                            <verbose>true</verbose>
                            <images>
                                <image>
                                    <alias>db-postgres</alias>
                                    <name>db-postgres</name>
                                    <build>
                                        <from>${docker.postgres.version}</from>
                                    </build>
                                    <run>
                                        <env>
                                            <POSTGRES_DB>${postgres.db}</POSTGRES_DB>
                                            <POSTGRES_USER>${postgres.user}</POSTGRES_USER>
                                            <POSTGRES_PASSWORD>${postgres.password}</POSTGRES_PASSWORD>
                                        </env>
                                        <ports>
                                            <port>${postgres.port}</port>
                                        </ports>
                                        <wait>
                                            <log>database system is ready to accept connections</log>
                                        </wait>
                                    </run>
                                </image>
                                <image>
                                    <alias>liquibase</alias>
                                    <name>liquibase</name>
                                    <build>
                                        <from>${docker.jdk.version}</from>
                                        <entryPoint>
                                            <exec>
                                                <args>java</args>
                                                <args>-jar</args>
                                                <args>${home.data.migration.jar.name}-${home.data.migration.jar.version}.jar</args>
                                                <args>--url=${postgres.url}</args>
                                                <args>--user=${postgres.user}</args>
                                                <args>--password=${postgres.password}</args>
                                            </exec>
                                        </entryPoint>
                                        <assembly>
                                            <basedir>/</basedir>
                                            <inline>
                                                <files>
                                                    <file>
                                                        <source>${project.basedir}/target/${home.data.migration.jar.name}-${home.data.migration.jar.version}.jar</source>
                                                        <outputDirectory>/</outputDirectory>
                                                    </file>
                                                </files>
                                            </inline>
                                        </assembly>
                                    </build>
                                    <run>
                                        <ports>
                                            <port>${liquibase.port}</port>
                                        </ports>
                                        <links>
                                            <link>db-postgres</link>
                                        </links>
                                        <wait>
                                            <log>Successfully released change log lock</log>
                                        </wait>
                                    </run>
                                </image>
                                <image>
                                    <name>${project.artifactId}</name>
                                    <build>
                                        <from>${docker.jdk.version}</from>
                                        <entryPoint>
                                            <exec>
                                                <args>java</args>
                                                <args>-jar</args>
                                                <args>${home.application.jar.name}-${home.application.jar.version}.jar</args>
                                            </exec>
                                        </entryPoint>
                                        <assembly>
                                            <basedir>/</basedir>
                                            <inline>
                                                <files>
                                                    <file>
                                                        <source>${project.basedir}/target/${home.application.jar.name}-${home.application.jar.version}.jar</source>
                                                        <outputDirectory>/</outputDirectory>
                                                    </file>
                                                </files>
                                            </inline>
                                        </assembly>
                                    </build>
                                    <run>
                                        <ports>
                                            <port>${home.application.port}</port>
                                        </ports>
                                        <links>
                                            <link>db-postgres</link>
                                        </links>
                                    </run>
                                </image>
                            </images>
                        </configuration>
                        <executions>
                            <execution>
                                <id>start</id>
                                <phase>pre-integration-test</phase>
                                <goals>
                                    <goal>build</goal>
                                    <goal>start</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>integration-test</goal>
                                    <goal>verify</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>${maven.dependency.plugin.version}</version>
                <executions>
                    <execution>
                        <id>copy</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>com.softserveinc.ita.homeproject</groupId>
                                    <artifactId>${home.application.jar.name}</artifactId>
                                    <version>${home.application.jar.version}</version>
                                    <type>jar</type>
                                    <overWrite>true</overWrite>
                                    <outputDirectory>${project.build.directory}</outputDirectory>
                                    <destFileName>${home.application.jar.name}-${home.application.jar.version}.jar</destFileName>
                                </artifactItem>
                                <artifactItem>
                                    <groupId>com.softserveinc.ita.homeproject</groupId>
                                    <artifactId>${home.data.migration.jar.name}</artifactId>
                                    <version>${home.data.migration.jar.version}</version>
                                    <type>jar</type>
                                    <overWrite>true</overWrite>
                                    <outputDirectory>${project.build.directory}</outputDirectory>
                                    <destFileName>${home.data.migration.jar.name}-${home.data.migration.jar.version}.jar</destFileName>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
